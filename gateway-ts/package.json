{
  "name": "slime-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "fastify": "^5.0.0",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
