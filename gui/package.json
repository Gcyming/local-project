{
  "name": "slime-gui",
  "version": "0.0.3",
  "description": "slime Electron GUI MVP (Phase 5)",
  "author": "slime",
  "repository": {
    "type": "git",
    "url": "https://github.com/Gcyming/local-project.git"
  },
  "main": "out/main/index.js",
  "dependencies": {
    "electron-updater": "^6.8.9",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@electron/fuses": "^2.1.3",
    "@types/electron": "^1.6.12",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "electron-vite": "^5.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.3.0"
  },
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "type": "tsc -p tsconfig.json --noEmit",
    "prepare-runtime": "node ../scripts/prepare-runtime.mjs",
    "dist:win": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-final && npm run prepare-runtime && electron-vite build && node ../scripts/retry-build.mjs",
    "dist:win:publish": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-final && npm run prepare-runtime && electron-vite build && node ../scripts/retry-build.mjs --publish always",
    "dist:linux": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-linux && npm run prepare-runtime && electron-vite build && electron-builder --linux --publish never -c.directories.output=release-linux",
    "dist:linux:publish": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-linux && npm run prepare-runtime && electron-vite build && electron-builder --linux --publish always -c.directories.output=release-linux"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
