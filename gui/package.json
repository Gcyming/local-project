{
  "name": "slime-gui",
  "version": "0.0.8",
  "description": "slime Electron GUI MVP (Phase 5)",
  "author": "slime",
  "repository": {
    "type": "git",
    "url": "https://github.com/Gcyming/local-project.git"
  },
  "main": "out/main/index.js",
  "dependencies": {
    "electron-updater": "^6.8.9",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@electron/fuses": "^2.1.3",
    "@types/electron": "^1.6.12",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "electron-vite": "^5.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.3.0"
  },
  "scripts": {
    "dev": "node ./scripts/dev-utf8.mjs",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "type": "tsc -p tsconfig.json --noEmit",
    "prepare-runtime": "node ../scripts/prepare-runtime.mjs",
    "preicons": "node ./scripts/make-notify-icon.mjs && node ./scripts/assert-appicon.mjs",
    "dist:win": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-final && npm run prepare-runtime && electron-vite build && npm run preicons && node ../scripts/retry-build.mjs && node ./scripts/assert-appicon.mjs --postbuild && node ./scripts/pack-cuda-runtime.mjs",
    "dist:win:publish": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-final && npm run prepare-runtime && electron-vite build && npm run preicons && node ../scripts/retry-build.mjs --publish always && node ./scripts/assert-appicon.mjs --postbuild && node ./scripts/pack-cuda-runtime.mjs",
    "dist:linux": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-linux && npm run prepare-runtime && electron-vite build && npm run preicons && electron-builder --linux --publish never -c.directories.output=release-linux && node ./scripts/assert-appicon.mjs --bundle release-linux/linux-unpacked",
    "dist:linux:publish": "node ../scripts/preflight-clean.mjs && node ../scripts/build-safety-check.mjs --out-dir=release-linux && npm run prepare-runtime && electron-vite build && npm run preicons && electron-builder --linux --publish always -c.directories.output=release-linux && node ./scripts/assert-appicon.mjs --bundle release-linux/linux-unpacked"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
